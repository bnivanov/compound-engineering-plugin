# MECE capability map: CE skills vs pstack, by product lifecycle stage

Sources: 35 `skills/*/SKILL.md`; CE loop `docs/plans/2026-09-09-1324-ce-s12-skill-dogfood-plan.md`;
pstack inventory `docs/plans/2026-09-09-1900-feat-opt-in-mode-and-pstack-fold-ins-plan.md`
Fold/Adapt/Skip + Never-fold tables (pstack tree read 2026-09-09).

Rule: each CE skill listed once (primary stage); secondaries in parens. Each pstack item listed once.
`ce-mode`, `ce-verify` = planned (U1/U5), not yet on disk.

Stages S0 transversal, S1–S7 sequential.

| Stage | CE skills (primary) | pstack capability → disposition | Gap / overlap |
|---|---|---|---|
| S0 Route / orchestrate / survive | `ce-start` front door; `ce-setup` health+config; `ce-handoff` continuity (recall secondary); `ce-undo` graded revert; `ce-proof` share specs/plans; `lfg` hands-off pipeline (spans S2–S6); planned `ce-mode` sticky router (build/fix/decide/ship playbooks as refs) | `poteto-mode` router+playbooks-as-refs → **Adapt** to `ce-mode`; `recall` → **Skip**, owned by `ce-handoff`+host; 23 `principle-*` → **Skip**, steering vocab only if inside `ce-mode`; `bro` → **Skip**, a turn not a skill; `show-me-your-work` audit, `reflect`/`recall` mining → **Never-fold**, Cursor transcripts/`create-skill` bound | OMP has no session-mode primitive; stickiness is convention + re-anchor `read skill://ce-mode`, kill if 0/3 (U3). No `/loop`, `subagent_type`, per-Task `model:`. |
| S1 Strategize / decide | `ce-strategy` STRATEGY.md; `ce-pov` graded verdict / oracle panel (also gates S3) | `interrogate` → **Skip**, `ce-pov`/`ce-code-review` panel stronger; `decide.md` playbook (planned, verdict→`ce-pov`) | No gap; `ce-pov` is decision owner. |
| S2 Discover / frame | `ce-ideate` diverge (which ideas); `ce-brainstorm` converge to requirements-only plan | `architect`, `arena` → **Skip**, owned by `ce-brainstorm` approaches / `ce-plan`; `figure-it-out` → **Skip**, would be 3rd planner beside `ce-plan`+`ce-mode` | Explicit non-fill: no third planner. |
| S3 Plan / specify | `ce-plan` (+planned `code-lineage-analyst` Preserve/Change/Avoid/Risk); `ce-doc-review` plan/spec lenses | `why` (lineage, found-vs-inferred) → **Adapt** to `ce-plan` research; `architect` plan-through-code → **Skip**, CE loop is brainstorm→plan→work | Lineage gated: existing code + defensive-shape/KTD-reversal/fragile only; never greenfield/Lightweight. |
| S4 Build / isolate / fix | `ce-work` waves (local `swarm` shape); `ce-debug` diagnosis loop; `ce-prototype` throwaway de-risk; `ce-worktree` isolation (+planned prune: classify safe/unsafe/current, ask, never `--force`/branch-delete) | `swarm` local → **Skip**, = `ce-work` waves; `swarm` cloud → **Never-fold**; `worktree-cleanup` → **Adapt** to `ce-worktree` prune; `investigation`/`bug-fix`/`forensics` → **Skip**, = `ce-debug`; `build.md`/`fix.md` playbooks (planned) → `ce-brainstorm?`→`ce-plan`→`ce-work` / symptom→`ce-debug` | No cloud fleet, no iOS-simulator prune. Fix→build escape only when multi-file competing designs (G1). |
| S5 Verify / review / harden | `ce-test-browser` web; `ce-test-xcode` iOS; `ce-dogfood` diff-scoped browser QA+personas (planned: consumes feature map); `ce-polish` live UX refine; `ce-code-review` (+planned `blast-radius` undeclared-readers persona); `ce-simplify-code` post-build cleanup; `ce-optimize` measured loop; planned `ce-verify` `create`/`refresh` → `.agents/skills/verify-<app>/` + feature map | `create/maintain-verification-skill`+feature map → **Fold** to `ce-verify`; `blast-radius` → **Fold** to review persona; `interrogate` Stage 3d/4/5 → **Skip** (incumbent panel); `interrogate`/`perf`/`hillclimb`/`eval`/`visual-parity` → **Skip** to `ce-debug`/`ce-optimize`/`ce-polish`/`ce-dogfood`/eval-cell; `tdd` → **Skip** (`ce-work` evidence table); `unslop`/`no-comments` → **Skip** (`ce-simplify-code` r8); `technical-writing` → **Skip** (host `writing`); `ship.md` polish tail → simplify→review | `ce-verify` never judges experience / writes dogfood report (boundary with `ce-dogfood`). Map-absent is normal; map-vs-app: app wins, record drift. |
| S6 Ship / land | `ce-commit`; `ce-commit-push-pr` (opt-in stack submit); `ce-resolve-pr-feedback`; `ce-babysit-pr` `target`/`stack-ready`/`stack-land` (settle≠merged, land = `gh stack merge` bottom-most settled prefix) | `babysit`/`shipping`/`autopilot`/`orchestrate` → **Skip**, = babysit postures + `lfg`; `ship.md` playbook (planned) → simplify→review→commit-push→babysit; Origin/`gh stack merge`/cloud fleet half → **Never-fold** (land auth already in `stack-land`); `watch-pr`/`orch`/`bootstrap.ts` → **Never-fold** (2nd watcher beside `pr-snapshot`) | `stack-land` is sole merge exception; `target`/`stack-ready` never merge. `deslop`/`control-cli`/`control-ui` → **Never-fold** (`cursor-team-kit`); `ce-verify` replaces control-skill need. |
| S7 Listen / learn / grow | `ce-sweep` Slack/Issues→`lfg`-ready plan; `ce-riffrec-feedback-analysis` recording→evidence; `ce-product-pulse` time-windowed signals (feeds S1); `ce-compound` capture; `ce-compound-refresh` audit; `ce-retune` corpus retune (needs A/B harness); `ce-explain` durable teaching artifact; `ce-promote` launch copy | `how`/`teach` → **Skip**, = `ce-explain`; `recall` mining → **Never-fold**; `show-me-your-work` → **Skip** (`lfg`/review report) | `make-bot-ui` (Grok+Tailscale) → **Never-fold**, product integration not workflow. `typescript-best-practices` `paths:` → **Never-fold**, no OMP auto-attach. |

MECE check: 6+2+2+2+4+7+4+8 = 35 existing skills, each once; +2 planned.
All pstack Fold rows (6) + Skip rows (~13) + Never-fold rows (9) each placed once.

# ce-start

The front door. Ask what you want to do; it routes to the right skill.

```text
/ce-start
```

Hidden skills are invoked as `/skill:<name>`. Visible skills use `/ce-start` itself and then `/ce-brainstorm`, `/ce-plan`, and so on.
